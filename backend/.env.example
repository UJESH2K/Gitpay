# GitHub Pay Backend Configuration

# Server
PORT=3000
NODE_ENV=development

# Solana Configuration
# Network: devnet, testnet, mainnet
SOLANA_NETWORK=devnet

# Solana private key (for escrow wallet)
# Generate a keypair: solana-keygen new
# Get the array format: solana-keygen show --outfile keypair.json (copy the entire array)
SOLANA_PRIVATE_KEY=[1,2,3,4,...] # Replace with your actual keypair array

# GitHub Configuration
# Create a GitHub App and get the token
# https://github.com/settings/apps/new
GITHUB_TOKEN=your_github_personal_access_token_here

# GitHub App webhook secret
GITHUB_WEBHOOK_SECRET=your_webhook_secret_here

# Backend URL (for webhook configuration)
BACKEND_URL=http://localhost:3000

# Database
DATABASE_PATH=./data/github-pay.db
